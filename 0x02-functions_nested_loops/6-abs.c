#include"main.h"
/**
 * _abs - function that computes the absolute value of an integer.
 * @i: type int paramiter
 *
 * Return: int
 */
int _abs(int i)
{
	if (i < 0)
		return (-i);
	else
		return (i);
}
